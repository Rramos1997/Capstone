#include "Manager.h"
#include <iostream>
#include <vector>
#include <stdexcept>

using namespace std;


//Commands i want the manager to do

void Manager::runManager(string command){
    //I will use C++ to parse out each word in command and then use bash script inside c++ to run the commands
    /*
    loop to parse command string into each element
    ex:
    vector<string> fullCommand;
    int i =0;
    while(parsed string == true)
        fullCommand.push_back(parsed string)
        i++;

    switch(fullCommand[0])
        case "compile":
            //bashscript
        case "move":
            //bashscript
        case "run":
            //bashscript
        default:
            assert(false)
    */
}
